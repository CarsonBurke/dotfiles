---
name: make-simple
description: Review changed code for unnecessary complexity and simplify it.
---

Review files in the current git diff for unnecessary complexity, then fix what you find. If `$ARGUMENTS` is a path, use that instead of the diff.

Examples: convoluted conditionals, excessive nesting, over-abstraction, dead code paths, unclear data flow — but use good judgement.
